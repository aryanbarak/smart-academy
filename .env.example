# Copy this file to `.env` and fill in your API key.
# Do NOT commit your real `.env` to source control.

# Google GenAI API key (server-side only)
API_KEY=your-google-genai-api-key-here

# Optional: port for the local server that proxies AI requests
SERVER_PORT=4000
